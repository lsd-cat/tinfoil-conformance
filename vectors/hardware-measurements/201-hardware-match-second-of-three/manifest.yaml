id: 201-hardware-match-second-of-three
stage: verify-hardware-measurements
title: |
  Three hw entries; enclave matches the second → accept and return that entry.
spec_refs: ["6.3"]
expects:
  exit_code: 0
required_capabilities: {}
fixture_kind: synthetic
notes: |
  SPEC §6.3 step 3: 'Iterate ... return the first match'.
  Catches an SDK that always returns measurements[0] regardless of value.
